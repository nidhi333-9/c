#include <stdio.h>

int main()
{
    if (1)
    {
        printf("This if is executed\n");
    }
    if (32)
    {
        printf("This if is also execued\n");
    }
    if (0)
    {
        printf("This if is not executed");
    }
    return 0;
}